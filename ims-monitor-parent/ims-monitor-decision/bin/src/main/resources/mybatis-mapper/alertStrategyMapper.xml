<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gwghk.ims.monitor.dal.mapper.AlertStrategyDao">
	<select id="findAlertStrategyByItemId" parameterType="java.lang.Long" resultType="AlertStrategy">
		select * from alert_strategy where enable=1 and item_id=#{itemId}
	</select>
</mapper>